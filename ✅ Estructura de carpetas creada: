
import os

# Crear la estructura de carpetas del proyecto
base_path = "/mnt/agents/output/bobbespoja-website"

folders = [
    "positive-page",
    "positive-page/html",
    "positive-page/css",
    "positive-page/js",
    "positive-page/assets/images",
    "positive-page/assets/videos",
    "positive-page/assets/audio",
    "positive-page/assets/fonts",
    "negative-page",
    "negative-page/html",
    "negative-page/css",
    "negative-page/js",
    "negative-page/assets/images",
    "negative-page/assets/videos",
    "negative-page/assets/audio",
    "negative-page/assets/fonts",
]

for folder in folders:
    os.makedirs(os.path.join(base_path, folder), exist_ok=True)

print("✅ Estructura de carpetas creada:")
for root, dirs, files in os.walk(base_path):
    level = root.replace(base_path, '').count(os.sep)
    indent = ' ' * 2 * level
    print(f'{indent}{os.path.basename(root)}/')
